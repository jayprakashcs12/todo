<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Create ToDo</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css" />
    <link rel="stylesheet" href="/css/style.css" />
</head>
  <body>
    <div class="input_container">
      <h1 class="pro-head">Todo</h1>
      <form action="/api/v1/todo" method="post" class="pro-form">
        <input type="text" name="name" class="pro-input" placeholder="Enter to-do value here">
        <button class="pro-btn pro-edit" type="submit"><i class="bi bi-send"></i></button>
      </form>

      <div class="todo_list">
        <h1 class="pro-head">List of todos</h1>
        <table>
          <tr>
            <th>ID</th>
            <th>Todo</th>
            <th colspan="2">Action</th>
          </tr>
          <% todos.forEach((todo, i)=> { %>
            <tr>
              <td>
                <%= i + 1 %>
              </td>
              <td>
                <%= todo.name %>
              </td>
              <td><a class="pro-btn pro-edit" href="/api/v1/todo/<%= todo._id%>"><i class="bi bi-pencil"></i></a></td>
              <td>
                <form action="/api/v1/todo/<%= todo._id%>?_method=DELETE" method="post" id="formdel">
                  <button class="pro-btn pro-del" type="submit"><i class="bi bi-trash3"></i></button>
                </form>
              </td>
            </tr>
            <% }) %>
        </table>
      </div>
    </div>
  </body>
</html>